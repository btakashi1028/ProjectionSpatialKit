# Projection Spatial Kit — 詳細ガイド

> **Status: 探索的リファレンス実装 / as-is・MIT・積極保守なし.**
> 「Unity 上で Unity アプリ（プロジェクタ / URG / モニタ会場）をどこまでシミュレーションできるか」を
> 個人で試した設計探索です。本番運用ツールではなく、Unity ランタイム前提でブラウザ完結もしません。
> 単一ディスプレイ・無改修コンテンツの範囲では動きますが、縦横混在のマルチディスプレイや実機忠実度は
> 制約があります。Issue / PR に即応する予定はありません。
>
> **実用寄りの使い方は「見て確かめる」より「計算で判定する」ほうです** — §8 の **Preflight 検査**は、
> 投影配置（着弾サイズ・スローレシオ適合・入射角・遮蔽）と画面構成（チャネル対応・解像度整合）を
> Edit モードのまま検算し、対処つきのレポートを出します。プレビューの制約を受けません。

会場（プロジェクタ / URG タッチセンサ / 液晶モニタ）のセットアップを、現地に行かずに
Unity 上で検証するためのエディタ拡張です。**コンテンツ側のシーンは無改修**のまま、キットが
入力を注入し・出力をキャプチャして会場を再現します。

このドキュメントは、**別プロジェクトへ `Assets/ProjectionSpatialKit` をインポートして
使い始めるまでの流れ**と、詳細設定、トラブルシュートをまとめたものです。

---

## 0. 前提条件

| 項目 | 要件 |
| --- | --- |
| Unity | 6000.3 系（開発・検証は 6000.3.11f1） |
| レンダーパイプライン | **URP 17.x**（Universal Render Pipeline）。ビルトイン RP では動きません |
| パッケージ | **Input System**（`com.unity.inputsystem`）が有効。Player Settings の *Active Input Handling* が **Input System Package**（または Both） |
| その他参照 | URP Core/Universal、UnityEngine.UI（asmdef が参照済み） |

> URP でない / Input System が無効だと、後述の「セットアップ診断」が警告を出します。

---

## 1. インポート

`Assets/ProjectionSpatialKit` フォルダ**丸ごと**を、対象プロジェクトの `Assets/` 配下に
コピーします（.meta も含める＝GUID を保つ）。フォルダ内で完結しており、他の場所に
散らばりません。

インポート後、Unity Consoleにコンパイルエラーがないことを確認します。

---

## 2. 初期セットアップ（1回だけ）

メニュー **`Projection Spatial Kit ▸ Run Project Setup`** を実行します。以下を診断し、
必要なら「修復」できます。

- **URP** が有効か
- **Input System** が有効か
- **会場レイヤー `SpatialKitVenue`** の登録（会場オブジェクト専用レイヤー）
- **投影シェーダ**（クッキー Blit）の *Always Included Shaders* 同梱（ビルド時のストリップ防止）
- **会場レンダラー**（ホストの既定レンダラーを複製した会場用。**既定レンダラーは変更しません**。
  Scene ビューも会場レンダラーで描きたい場合のみ、オーバーレイのトグルで opt-in）

> これらはホストプロジェクトの設定を**破壊しません**（レイヤー追加・シェーダ同梱・レンダラー
> 追記のみ）。既定レンダラーの差し替えは明示的な opt-in のときだけ行われます。

Scene ビュー右側の **「Projection Spatial Kit」オーバーレイ**（`` ` `` キーの Overlays メニューで
表示切替）の「セットアップ」セクションでも、同じ診断と「修復 / すべて修復」ができます。

---

## 3. 会場シーンを作る（メインの導線）

**投影したいコンテンツのシーンを開いた状態**で、オーバーレイの
**「この開いているシーンから会場を作る」**を押します。

- いま開いているシーンを「コンテンツ」とみなし、それを投影する**別ファイルの会場シーン**
  `<シーン名>_Venue.unity` を新規作成します。
- 会場シーンには Room（部屋）・Projection Set（プロジェクタ+URG）・Observer カメラ・
  Simulator が入り、`Content Scene` に元シーンのパスが設定済みで保存されます。
- コンテンツ側のシーンは**一切変更しません**。

> ⚠️ **コンテンツシーン自身に会場を追加しないでください**。会場が自分のシーンを加算ロード
> し続けて無限生成になります（ガードで停止しエラーを出します）。会場は必ず別シーンに。

空のシーンに会場だけ入れたい上級者向けには、オーバーレイ「詳細」の
**「この空シーンに会場だけ追加」**（メニュー `Add Venue To Current Scene`）もあります。

---

## 4. 出力機材の設定

オーバーレイの **「出力機材」** に、置いた機材が1行ずつ並びます。

```
[機材名(選択)]  →  [映す画面 ▼]   [タッチ]   [削除]
例:  Projector          → 画面 1     タッチ:URG   削除
     Scoreboard Monitor → 画面 2     ☐ タッチ    削除
```

- **行の名前** = 機材そのもの（`Monitor 1` / `Projector 2` など）。
- **「→ 画面 N」** = その機材が映す **Content Display**。ここでコンテンツの何画面目を
  出すか選びます。
- **「モニタを追加」「プロジェクタを追加」** で機材を増やせます（名前は連番、位置も少し
  ずらして重ならないようにします）。

> 📌 **画面番号の注意**: 「画面 1」＝ Unity の Display 1（内部インデックス 0）。UI は常に
> 1 始まりの「画面 N / Display N」で表示するので、0/1 の暗算は不要です。映す画面の
> **解像度など画面自体の設定は Simulator** 側（後述）で行います。

### プロジェクタのタッチ

プロジェクタ単体は**表示専用**です。その投影面をタッチ可能にするには、同じ Projection Set
に **URG**（タッチセンサ）が必要です（「タッチ:URG」表示になります）。投影面は
オーバーレイ「投影面 ▸ 壁をクリックして投影面を設定」→ 部屋の面をクリックで再配置できます。

### モニタのタッチ（つまずきやすい）

モニタは**既定で表示専用**。タッチさせるには **どちらか**：

1. 出力機材のそのモニタ行の **「タッチ」トグルを ON**（= Force Touch Enabled）。**Play 中の
   切り替えも即時反映**されます。
2. **タッチ対応の Monitor Device Profile**（`Is Touch Panel = ON`）を割り当てる。

そのうえで、**Observer ビューでそのモニタが見える位置**からパネル面をクリックします
（ベゼルや画像外はタッチになりません）。

---

## 5. コンテンツ画面（チャネル）と縦画面

**画面（チャネル）＝会場が再現するコンテンツ Display とその解像度**は、Scene オーバーレイの
**「画面 (チャネル)」セクション**で直接編集できます（画面番号・解像度・横/縦・追加/削除）。
「出力機材」（機材→画面・タッチ）のすぐ下に並ぶので、**どの機材がどの画面を・どの解像度で・
タッチ有無で出すか**を1画面で把握できます。

- **縦画面（ポートレート）**: チャネル解像度を例えば **1080×1920** にするだけ。モニタは
  **Match Content Aspect（既定 ON）**でパネル形状をコンテンツ比に合わせ、回転も歪みも
  無く縦表示します。
- **解像度は実機ディスプレイ / Game View と一致**させてください。ズレると表示とタッチ位置が
  食い違います（入力座標はチャネル解像度基準で注入されます）。

さらに細かい設定は「シミュレーション ▸ Simulator を選択」で **Simulator** のインスペクタから：

- **Content Scene**: 投影するシーンを **SceneAsset をドラッグ&ドロップ**で指定（パス手打ち不要）。
- **Output channels**: オーバーレイの「画面 (チャネル)」と同じもの（どちらで編集しても可）。
- **Preset File**: 設定をアセットに保存/読込（明示ボタンのみ。割り当てただけでは何も
  変わりません）。

> モニタを「まだチャネルの無い画面番号」に向けると黒表示になります。その画面用チャネルを
> 「画面 (チャネル)」に追加してください。

> **入力は New Input System 前提**（Active Input Handling は Both でも可）。キットが生成する
> 仮想 Touchscreen / Mouse は**旧 `Input.*` には届きません**。コンテンツ側は New Input System、
> UI は *Input System UI Input Module* で入力を読んでください。

---

## 6. 実行と確認

オーバーレイ「シミュレーション ▸ 再生」、または通常どおり Play します。

- **コンテンツ**は Display 1、**Observer（会場を見るカメラ）**は Display 3
  （`targetDisplay = 2`）に出ます。
- Observer ビューで投影面 / モニタをクリックすると、タッチがコンテンツへ注入されます。
- 「デモ入力を再生」で無人の自動タッチ（ScriptedDemo）も流せます。

---

## 7. うまくいかない時のチェックリスト

| 症状 | 原因 / 対処 |
| --- | --- |
| 会場が無限に増える | コンテンツシーン自身に会場を入れた。→ 別シーンで「この開いているシーンから会場を作る」 |
| モニタのタッチが無反応 | モニタが表示専用のまま。→ 行の「タッチ」ON か、タッチ対応プロファイル。Observer ビューで見えているか、画像面をクリックしているかも確認。コンテンツが New Input System で入力を読んでいるかも確認（旧 `Input.*` には届きません） |
| Display2 等のタッチが Display1 に化ける / 位置がずれる | 注入する TouchState/MouseState には displayIndex を設定し、座標はチャネル解像度基準で算出しています。ビルドや実機では一致しますが、**エディタでは対象ディスプレイの Game View 解像度をチャネル解像度に合わせてください**（ずれると UI レイキャストが外れて反応しないことがあります） |
| 縦横を同時に混在させた多ディスプレイがエディタでうまく出ない | Editor の Game View は一度に1解像度のため、表示中でない側がズーム/崩れて見えます。確認したいディスプレイに切り替え、その Game View 解像度をチャネルに一致させてください（ビルドでは各ディスプレイが実解像度なので問題になりません） |
| モニタが黒い | 向けた「画面 N」に対応するチャネルが無い。→ オーバーレイ「画面 (チャネル)」に追加 |
| 縦画面が横に潰れる/回る | チャネル解像度を縦（例 1080×1920）に。Match Content Aspect は ON のまま |
| Game View と会場の見た目が違う | Game View の解像度プリセットは自動では変わりません。表示中の Display の解像度をチャネルと同じ値に手動設定してください |
| Project Setup 後に URP レンダラー参照が壊れて見える | 最新版は Run Project Setup 実行時に欠損・重複会場エントリを自動修復します（既定レンダラーは維持） |
| 投影が左右反転 | リアプロジェクション用のミラーが ON。フロント投影なら Projector の `Mirror Cookie Horizontally` を OFF |
| Display3 をクリックするとフォーカスが Display1 に飛ぶ | 旧版の不具合。最新版では解消済み（Input System 設定を書き換えない） |
| 音の「AudioListener が2つ」警告 | コンテンツ読込後にキットが自動で1つに（会場側を無効化）。出なければ OK |
| Scene に URP でない旨の警告 | ホストが URP でない。URP へ移行が必要 |
| コンテンツにホストのフルスクリーン描画が乗らない | 会場は既定レンダラーを使うため。オーバーレイの「Scene ビューを会場レンダラーで描画」を検討、または Simulator の Content Camera Renderer Index を設定 |

---

## 8. Preflight 検査（現地入り前の判断支援）

メニュー **`Projection Spatial Kit ▸ Preflight Check`**（Scene オーバーレイの「Preflight 検査」ボタンでも可）で、
**投影配置と画面構成を計算で検査**し、**要対応 / 警告 / 情報**と**数値つきの対処**を一覧します。
Play も描画も不要で、Edit モードのまま走ります。

検査する内容:

**投影配置** — 像が投影面に収まるか（4隅の着弾）/ 目標画像幅とのズレと必要な前後移動量 /
**必要スローレシオが機種の範囲内か**（範囲外なら「短焦点機が必要」「◯m 後退」と提示）/
レンズシフトが機種の上限を超えていないか / 入射角（台形歪み）/ フォーカス距離のズレ /
ビーム上の遮蔽物が像の何 % を遮るか。

**画面構成** — 機材が割り当てた画面にチャネルがあるか（無ければ黒表示）/ チャネルはあるが機材が無い /
縦横の向き不一致 / パネル比とチャネル比の不一致 / Observer と コンテンツ画面の Display 衝突 /
コンテンツシーンの Build Settings 登録 / **Game View 解像度とチャネル解像度の不一致**。

結果は **Markdown でコピー / 保存**できます。現地へ持参する、施工担当へ渡す、チケットに貼る、といった用途向けです。

> **できないこと**: 実輝度と環境光下の視認性、実センサのノイズ・遅延、色再現、複数 PC 同期。
> Preflight は「机上で確実に潰せる誤りを潰す」ものであって、**実機検証の代替ではありません**。
> なお入射角が大きい場合、スローレシオによる機種適合の判定は**保留**します（角度が付いた状態では
> スローレシオの規格値が当てにならないため。先に角度を直してから再検査してください）。

---

## 9. 動作を体験する（サンプル）

キット単体で挙動を見たい場合は、次の手順でサンプルを生成・再生します。

1. メニュー **`Projection Spatial Kit ▸ Bootstrap Sample Content Scene (Touch Ripple)`** を実行
   （タッチで波紋・ボールが出るコンテンツ `SampleContent_TouchRipple.unity` を生成）。
2. 続けて **`Projection Spatial Kit ▸ Bootstrap Sample Venue Scene`** を実行
   （それを投影する会場 `910_SampleVenue.unity` を生成）。
3. 生成された **`910_SampleVenue`** を開いて Play。Observer ビュー(Display 3)で投影面／モニタを
   クリックすると、コンテンツにタッチが注入されます。

> **重要（初回に投影が出ないとき）**: 会場はコンテンツシーンを **additive ロード**します。
> そのため**コンテンツシーンが Build Settings に登録**されている必要があります。上のブートストラップは
> 自動で登録しますが、手動でコンテンツシーンを差し替えた場合などは未登録になり得ます。その場合、
> **初回(コールド)オープンで投影が黒いまま／ビルドで読み込めない**という症状になります。
> Scene オーバーレイの「セットアップ」に **「コンテンツシーン (Build Settings)」チェックと修復ボタン**が
> 出るので、そこから登録してください（コンソールにも警告が出ます）。一度コンテンツシーンを開いてから
> 会場へ戻ると直る、という現象も同じ原因です。

---

*このガイドは実プロジェクト投入時のつまずきを洗い出すためのものです。手順で詰まった箇所・
用語が分かりにくい箇所があれば、その位置を添えてフィードバックしてください。*
