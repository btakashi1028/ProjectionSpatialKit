namespace ProjectionSpatialKit
{
    /// <summary>
    /// Physical mounting orientation of an output device. Portrait = the device is
    /// installed rotated 90° clockwise, so the content's horizontal axis runs vertically
    /// (the content itself keeps rendering in its own native orientation).
    /// </summary>
    public enum DisplayOrientation
    {
        Landscape,
        Portrait
    }
}
